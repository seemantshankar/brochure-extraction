You are analyzing a cropped image of a region from a vehicle brochure (e.g. a table, a list of specifications, a drawing, or a grid).
Your task is to refine the bounding box of the main element within this crop, returning the tightest possible bounding box that contains the actual content of the element, including its headers, text, borders, or lines, but excluding any outer padding, margins, or background noise.

Return:
- label: short text describing the refined region (taken from its header if present, or generic label)
- region_type: one of [ruled_table, section_grouped_table, bullet_panel, swatch_grid, stat_cards, technical_drawing, icon_badge, footnote_block, section_heading, other]
- bbox: [x0, y0, x1, y1] normalized to a 0-1000 scale relative to this cropped image. This box must tightly wrap the main element.
- may_contain_subregions: set to false

Return via the provided tool call only. No prose.
